name: Remote Deploy

on:
  push:
    branches:
      - main # Wird erst ausgelöst, wenn der PR gemergt wurde

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Telegram Start
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            🚀 *Deployment gestartet!*
            Version: ${{ github.sha }}
            Projekt: NOEXITFinance2.0
            
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: ./deploy.sh # Führt dein bestehendes deploy.sh auf dem Server aus

      - name: Notify Telegram Success
        if: success()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            ✅ *Deployment erfolgreich!*
            Version: ${{ github.sha }}
            Die App ist jetzt live unter:
            https://nefinanzen.duckdns.org

      - name: Notify Telegram Failure
        if: failure()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            ❌ *Deployment fehlgeschlagen!*
            Bei Version ${{ github.sha }} gab es Probleme.
            Bitte prüfe die GitHub Actions Logs.